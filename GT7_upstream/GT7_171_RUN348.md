# GT7 1.71 / Claude patch / probe 348 - 21 September 2026

Launch `GT7_probe348_171.bat`. Read-only preflight: add `-CheckOnly`.

The launcher imports all 76 GT environment assignments from probe336 and uses
the existing `run_gt7.ps1 -Offline` with the current GT7 lane executable.
It selects the full `CUSA24767` directory, avoiding the ambiguous `CUSA24~1`
alias. It does not execute probe336's dealer rotation or save checks: those
tools still target 1.00. The existing probe336 and Claude wrapper are unchanged.

## Verified inputs

- Game metadata: `CUSA24767`, `APP_VER=01.71`, plain ELF, 105193289 bytes.
- All seven sites in Claude's `C:\GT7_offline\gt7online.py` are patched.
- Reconstructing those seven patches from `eboot.bin.orig` exactly reproduces
  the live eboot; there are no other changes relative to that backup.
- Patched SHA256: `c38d4cc61ed1be6b7089891a05a1cd522b5e27e3064c0634a145385f6badfc91`.
- Original SHA256: `65f75f6bd1c30f003f2dacf322e577975ad8d06a1de89140e58d96fe43dc1f2c`.
- Emulator: `C:\shadps4-gt7\Build\x64-Clang-RelWithDebInfo\shadps4.exe`,
  modified 2026-09-21 20:38:37. Repository HEAD at inspection: `b1cd966e`.
  The binary's embedded description still mentions `4ca72e6c-dirty`; do not
  use that string alone to claim exact source provenance. The run manifest
  records the executable SHA256, runner hash, and baseline hash.

Preflight requires the verified eboot hash. Revalidate and update it if Claude
changes the patch. This run did not alter game files or emulator source.

## Hidden boot smoke test

Artifacts: `logs\probe348_171_20260921_213502_966\`.

- Ran on a separate, unshown Windows desktop with real Vulkan rendering.
- Log confirms `Game id: CUSA24767` and `App Version: 01.71`.
- The game rendered a PSN sign-in warning with an OK button; see `psn_dialog.png`.
- No input was sent. The test stayed at that dialog for approximately 75 seconds
  and was explicitly stopped by Codex. The recorded exit `-1` is this controlled
  termination, NOT evidence of a game crash. `controlled_stop.txt` records it.
- No `SurfaceFormat` assertion was observed at this stage. Since the dialog was
  not dismissed, this does NOT establish that the previous graphics assertion
  or later offline/online checks have been fixed.
- `fatal.txt` records a Windows CoreUI C++ exception on the hidden desktop.
  The emulator continued rendering thousands of frames afterwards; it was not
  the terminating event. Keep this environmental limitation when comparing runs.
- The runner mentions an existing `device_fault.bin`; its timestamp is 20 Sep
  22:41:15, before this test. It is not evidence from this run.
- `last_crumb.txt` describes the previous run, as its own header states.
- Original 1.71 savedata and pre-run config/users files are backed up in the
  run directory. The latest eboot hash passed preflight again after the test.

Next manual check: launch probe348, dismiss the PSN warning with OK, and observe
the next screen. Full offline gameplay, local saving, and dealership behavior
on 1.71 remain unverified. The local-save and dealer work for 1.00 must not be
reported as part of this seven-site 1.71 patch.
